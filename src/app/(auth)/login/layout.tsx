function LoginLayout({ children }: { children: React.ReactNode }) {
  return (
    <section className="flex w-full h-lvh justify-center items-center bg-white">{children}</section>
  );
}

export default LoginLayout;
